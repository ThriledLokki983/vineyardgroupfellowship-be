-- Database initialization script for Vineyard Group Fellowship
-- This script sets up the initial database configuration

-- Ensure the database exists (this is already handled by POSTGRES_DB env var)
-- CREATE DATABASE IF NOT EXISTS vineyard_group_fellowship;

-- Create any additional database objects or configurations here
-- For now, we'll just add some comments for future use

-- Extensions that might be useful for the application
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";    -- For UUID generation
-- CREATE EXTENSION IF NOT EXISTS "pg_trgm";      -- For trigram matching (search)
-- CREATE EXTENSION IF NOT EXISTS "unaccent";     -- For accent-insensitive search

-- You can add initial data or configuration here
-- This script runs when the database is first created